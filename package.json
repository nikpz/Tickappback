{
  "name": "tickup-backend",
  "type": "commonjs",
  "version": "1.0.0",
  "description": "Socket.io + Sequelize backend for Tickup (no REST, no Redis).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "seed": "node seed.js",
    "dev": "nodemon server.js",
    "erd": "sequelize-erd --source ./images --destination ./images/erd.svg"
  },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "pg": "^8.10.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.32.1",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22",
    "sequelize-cli": "^6.6.3",
    "sequelize-erd": "^1.3.1"
  },
  "engines": {
    "node": ">=14"
  }
}
